-- Versioned seed: executes once. No repeatable seed, UPDATE, REPLACE or upsert.
-- Source: current BE Java createDefault(), NOT the older phase2 SQL.
-- Keep user/learner tables empty; per-user defaults must come from current policy at creation time.
-- Initial audit timestamps are UTC, with no change to JDBC session timezone.

INSERT INTO language_learning_admin_setting (
    id,
    default_daily_sentence_count,
    min_daily_sentence_count,
    max_daily_sentence_count,
    daily_keyword_max_count,
    review_available_days,
    level_recheck_recommendation_days,
    adaptive_writing_enabled,
    ai_evaluation_enabled,
    speaking_enabled,
    speaking_evaluation_enabled,
    default_daily_speaking_goal_minutes,
    min_daily_speaking_goal_minutes,
    max_daily_speaking_goal_minutes,
    daily_speaking_hard_limit_minutes,
    daily_speaking_session_limit,
    max_session_minutes,
    max_turns_per_session,
    min_valid_audio_seconds,
    max_turn_audio_seconds,
    max_audio_file_bytes,
    raw_audio_retention_days,
    reported_audio_retention_days,
    active_session_resume_hours,
    automatic_retry_limit_per_stage,
    manual_retry_limit_per_stage,
    stt_timeout_seconds,
    tts_timeout_seconds,
    evaluation_timeout_seconds,
    level_test_question_pool_target_size,
    level_test_question_pool_replenishment_enabled,
    created_by,
    created_at,
    updated_by,
    updated_at
) VALUES (
    'DEFAULT',
    5,
    1,
    20,
    5,
    7,
    30,
    TRUE,
    TRUE,
    TRUE,
    TRUE,
    5,
    3,
    20,
    30,
    5,
    10,
    20,
    1.0,
    60,
    10485760,
    7,
    30,
    2,
    2,
    1,
    30,
    30,
    60,
    1000,
    FALSE,
    'SYSTEM',
    UTC_TIMESTAMP(6),
    'SYSTEM',
    UTC_TIMESTAMP(6)
);

INSERT INTO language_learning_listening_policy_setting (
    id,
    enabled,
    default_item_count,
    min_item_count,
    max_item_count,
    hard_item_limit,
    reference_audio_max_seconds,
    repeat_audio_max_seconds,
    max_audio_file_bytes,
    max_rerecord_count,
    resume_hours,
    reference_audio_retention_days,
    user_audio_retention_days,
    reported_audio_retention_days,
    automatic_retry_limit,
    manual_retry_limit,
    practice_attempt_limit,
    profile_policy_version,
    model_config_version,
    reference_tts_regeneration_enabled,
    created_by,
    created_at,
    updated_by,
    updated_at
) VALUES (
    'DEFAULT',
    TRUE,
    5,
    1,
    20,
    30,
    30,
    60,
    10485760,
    2,
    2,
    7,
    7,
    30,
    2,
    1,
    1,
    'listening-profile',
    'listening-model-config',
    FALSE,
    'SYSTEM',
    UTC_TIMESTAMP(6),
    'SYSTEM',
    UTC_TIMESTAMP(6)
);
